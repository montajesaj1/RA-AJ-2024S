import { ConfigYaml } from '@verdaccio/types';
export declare function resolveLogo(config: ConfigYaml, req: any): string;
export default function renderHTML(config: ConfigYaml, manifest: any, manifestFiles: any, req: any, res: any): void;
