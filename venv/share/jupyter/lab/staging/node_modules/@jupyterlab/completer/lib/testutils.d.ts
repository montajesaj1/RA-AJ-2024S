import { CodeEditorWrapper } from '@jupyterlab/codeeditor';
export declare function createEditorWidget(): CodeEditorWrapper;
/**
 * jsdom mock for getBoundingClientRect returns zeros for all fields,
 * see https://github.com/jsdom/jsdom/issues/653. We can do better,
 * and need to do better to get meaningful tests for rendering.
 */
export declare function getBoundingClientRectMock(): {
    toJSON: () => {
        width: number;
        height: number;
        top: number;
        left: number;
        x: number;
        y: number;
        bottom: number;
        right: number;
    };
    width: number;
    height: number;
    top: number;
    left: number;
    x: number;
    y: number;
    bottom: number;
    right: number;
};
