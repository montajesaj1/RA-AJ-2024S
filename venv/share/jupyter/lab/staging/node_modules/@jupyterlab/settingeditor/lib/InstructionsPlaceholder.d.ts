import { ITranslator } from '@jupyterlab/translation';
type ISettingsEditorPlaceholderProps = {
    translator: ITranslator;
};
export declare const SettingsEditorPlaceholder: ({ translator }: ISettingsEditorPlaceholderProps) => JSX.Element;
export {};
